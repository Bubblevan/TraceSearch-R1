"""Explicit, non-shadowing FlashAttention compatibility helpers.

This namespace is intentionally ``tracesearch.compat.flash_attn`` rather than
``flash_attn``.  The real external package must win whenever it is installed.
"""
